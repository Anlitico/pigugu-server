# Proper-noun vocabulary the STT decoder must hear correctly.
#
# One term per line — add a term by adding a line (no code change). Terms are
# read once at startup, so a new line takes effect after the agent restarts.
# Blank lines are ignored; a '#' starts a comment anywhere in a line, so a term
# never carries one.
#
# Keep terms specific and short (<50 chars) — keyterm prompting over-corrects
# on common words (AssemblyAI / Deepgram keyterm docs).

Pigugu
Trump

# "roast" is the game trigger word. STT has been mis-hearing it as "rows",
# which silently kills the roast-by-request gate (the LLM never sees "roast").
roast
